version: 3
projects:
  - name: terraform-root
    dir: terraform
    workflow: default
    autoplan:
      enabled: true           
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "modules/**/*.tf"
      apply: false         

