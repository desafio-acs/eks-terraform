version: 3
projects:
  - name: terraform-root
    dir: terraform
    terraform_version: v1.9.2
    autoplan:
      enabled: true           
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "modules/**/*.tf"  

